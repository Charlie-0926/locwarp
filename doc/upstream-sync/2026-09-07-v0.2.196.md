# v0.2.196 上游同步報告

日期：2026-09-07

## 同步目標

- 官方 remote：`https://github.com/keezxc1223/locwarp.git`
- 客製 fork：`https://github.com/Charlie-0926/locwarp.git`
- 官方 release tag：`v0.2.196`
- 正規化版本：`0.2.196`
- 官方 tag／`upstream/main` commit：`271779bef895c2443ebde4cdbe3e35bbf72be6de`
- 官方 manifest：`upstream/main:frontend/package.json` → `0.2.196`
- 同步分支：`sync/upstream-v0.2.196`

本次同步以官方 `upstream/main` head 為準；官方 tag 與 manifest 已通過一致性檢查。

## Preflight 與官方變更

- 初始工作分支：`custom/main`
- 正式合併前客製基底：`2d54421`（包含本次同步前的文件進度提交）
- 原始工作樹有既存 `doc/progress.md` 變更，已安全保留並以文件提交保存，未丟棄使用者內容。
- remote 契約符合 `upstream = keezxc1223/locwarp`、`origin = Charlie-0926/locwarp`。
- 抓取後分歧：客製 `24` commits／官方 `4` commits。

官方新增範圍：

1. `1a2e830`（`v0.2.193`）：WiFi RemotePairing 自動尋找可用 port。
2. `c96a82d`：安裝程式歡迎頁與視覺素材。
3. `f9fa2c8`（`v0.2.195`）：安裝程式歡迎／完成頁文字調整。
4. `271779b`（`v0.2.196`）：`pymobiledevice3` 升至 11.2.0、Electron／MapLibre 等前端相依升級，以及 PyInstaller 對 `pmd_pytcp` userspace tunnel 的收集。

## Rehearsal 與衝突處理

`scripts/rehearse-upstream.ps1 -SkipFetch` 已執行，偵測到：

- `backend/api/device.py`
- `backend/requirements.txt`
- `frontend/package-lock.json`

正式同步在 `sync/upstream-v0.2.196` 完成下列整合：

- `backend/api/device.py`：採官方 port × UDID retry、期限控制與重新掃描流程；保留客製 `WifiTunnelRuntimeError` 對 Python 3.13/TLS-PSK 缺失的明確 `python313_missing` API 錯誤映射。重新掃描區塊不再誤用已完成 handshake 的 runner。
- `backend/requirements.txt`：採官方 `pymobiledevice3>=11.2.0`、FastAPI `>=0.141.1`、Uvicorn `>=0.52.4` 與 Pydantic `>=2.13.5`，不因應用程式版本改動 `backend/main.py` 的 FastAPI schema 版本。
- `frontend/package-lock.json`：採官方 v0.2.196 相依圖，清除舊 v0.2.192／Rolldown optional lock 片段，再以 `npm install --package-lock-only --ignore-scripts` 依合併後 manifest 重新生成。官方已有 `@types/supercluster` devDependency，因此移除客製 manifest 中重複的 runtime 宣告；客製功能不依賴該宣告。

沒有移除 Spiral、Jump Random Walk、2-opt、multi-device、single-instance 或速度持久化的客製實作；官方核心更新仍透過既有 adapter／extension 接線整合。

## 版本一致性

執行官方規範的 `npm version 0.2.196 --no-git-tag-version --allow-same-version` 後，三個本地值為：

- `frontend/package.json`: `0.2.196`
- `frontend/package-lock.json` 頂層 `version`: `0.2.196`
- `frontend/package-lock.json` `packages[""].version`: `0.2.196`

三者、官方 tag 正規化版本與官方 manifest 完全一致。

## 客製功能稽核

- Spiral Mode：backend extension、API adapter、frontend extension/state 接線保留。
- Jump Random Walk：dwell／segment transition 套用、pause、stop、hot apply 與前端設定接線保留。
- 2-opt：固定起點、完整不重複 waypoint order、O(1) 增量候選評估、非對稱 duration matrix、`None` 不可達邊拒絕，以及對稱／非對稱 first-improvement oracle 保留。
- multi-device：coordinator、runtime、fan-out、UI mirroring、state alignment 與 leader handoff 接線保留。
- single-instance protection、跨 waypoint 速度維持、Pyright／Mypy／async 修正保留。
- PyInstaller：客製 Python 3.13/TLS-PSK runtime guard 與官方 `pmd_pytcp` 收集並存。

## 驗證結果

下列結果在解決衝突後執行：

- [x] `scripts/check-extension-boundaries.ps1`（通過）
- [x] `scripts/verify.ps1`（boundary、Pyright 0/0/0、Electron syntax、TypeScript、Vite production build 通過；僅既有 warning）
- [x] `backend/tests/extensions/test_route_optimizer.py`（5 passed；以 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` 避開 venv xonsh console plugin）
- [x] Python tests／Mypy（backend 全部 19 passed；Mypy 61 source files 無 issues）
- [x] `git diff --check` 與 conflict-marker scan
- [x] 最終 upstream rehearsal：`custom/main` `6831504` + `upstream/main` `271779b` 無衝突

`doc/coordinates.txt` 不加入 Git、CI fixture 或報告；只在需要時記錄彙總 benchmark 指標。

## 整合與發布

- merge commit：`3d271cfb888941459709aea1175f1fda4cfde3d8`
- merge parents：客製 `2d54421`、官方 `271779b`
- `custom/main` fast-forward：已完成至 `6831504`（`--ff-only`）
- `origin/main`／`origin/custom/main` push：需使用者授權後執行
- `Custom compatibility` GitHub Actions：push 後監看並記錄 URL

## 尚待實機驗證

CI 無法取代 Windows／iPhone 實機確認：`pymobiledevice3` 11.2.x 的 USB／WiFi tunnel、PyInstaller bundle、兩台 iPhone 的 multi-device auto-sync、UI mirroring、state alignment、leader handoff，以及 Jump dwell／固定方向移動在 route transition、pause、hot-apply 下的行為。

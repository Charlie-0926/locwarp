# v0.2.192 上游同步報告

日期：2026-07-30

## 同步目標

- 官方 remote：`https://github.com/keezxc1223/locwarp.git`
- 客製 fork：`https://github.com/Charlie-0926/locwarp.git`
- 上次官方基線：`v0.2.190`（`c1a6c36`）
- 最新官方 tag：`v0.2.192`（`aec015d`）
- 本次納入的官方 head：`a1ebe2043dac42d139ef18a3e2914d0e24d49b82`

`upstream/main` 在 `v0.2.192` 後另有一個 README 更新，因此本次同步以官方 branch head `a1ebe20` 為準，而非只停在 tag commit。

## Preflight 與官方變更

- 初始分支：`custom/main`
- 初始工作樹：乾淨
- remote 契約：符合 `upstream = keezxc1223/locwarp`、`origin = Charlie-0926/locwarp`
- 抓取後分歧：客製 16 commits／官方 3 commits

官方新增 commits：

1. `8113556`（`v0.2.191`）：書籤／路徑右鍵「移動到」選單過長時支援捲動。
2. `aec015d`（`v0.2.192`）：升級前後端相依套件，包含 Electron 43、React 19、Vite 8、pymobiledevice3 10.1、websockets 16 等。
3. `a1ebe20`：補齊中英文 README 的 v0.2.174～v0.2.192 現況與相依版本資訊。

主要變更範圍為 README、`backend/requirements.txt`、PyInstaller spec、frontend package manifests／lockfile，以及 `BookmarkList.tsx`、`RouteList.tsx`、`ControlPanel.tsx`。

## 執行狀態

- [x] 讀取同步規範、客製功能帳本及相關腳本
- [x] 檢查分支、工作樹、remotes、歷史與分歧
- [x] 抓取官方 `main`、tags 並整理變更
- [x] 執行 merge rehearsal
- [x] 建立正式 `sync/upstream-v0.2.192` 整合分支
- [x] 驗證客製功能與完整測試
- [x] fast-forward `custom/main`
- [x] 最終 rehearsal
- [x] 推送 `main`、`custom/main`
- [x] 監看 `Custom compatibility` GitHub Actions

## Rehearsal 結果

`scripts/rehearse-upstream.ps1 -SkipFetch` 已執行並偵測到下列內容衝突：

- `README.md`
- `README.en.md`
- `backend/requirements.txt`

`backend/locwarp-backend.spec`、`frontend/package.json`、`frontend/src/components/ControlPanel.tsx` 等其餘官方變更可由 Git 自動合併。正式同步將留在產生的 `sync/upstream-v0.2.192` 分支，依官方文件／相依升級意圖與客製功能帳本處理上述衝突。

## 衝突處理

- `README.md`、`README.en.md`：採用官方合併後的「多點路徑 / Multi-point Route」名稱，保留客製版最新的到站後等待、15 km/h 固定方向直線移動、可設定移動秒數、pause／hot-apply 及最後路徑點精確停留說明。
- `backend/requirements.txt`：採用官方 v0.2.192 的 FastAPI、Uvicorn、websockets、Pydantic、python-multipart、psutil 版本下限；`pymobiledevice3` 使用官方目標版 `10.1.0`，但維持 exact pin，以保留客製 Python 3.13／PyInstaller 打包的可重現性。
- 沒有移除任何客製 extension 或功能 patch。
- `git diff --check` 與衝突標記掃描均通過。

## 相依環境校正

官方 v0.2.192 的 `frontend/package-lock.json` 無法直接執行 `npm ci`，原因為：

- 缺少 `@emnapi/core` 與 `@emnapi/runtime`。
- `@emnapi/wasi-threads` 的 lock 版本不符合相依圖。
- lockfile 頂層 package version 仍為 `0.2.191`，與 `package.json` 的 `0.2.192` 不一致。

已執行 `npm install` 校正 lockfile，接著重新執行乾淨 `npm ci` 並成功。實際安裝版本：

- Electron 43.2.0
- React／React DOM 19.2.8
- TypeScript 7.0.2
- Vite 8.1.5

npm audit 回報 18 個 high severity 項目；本次未執行可能導致 breaking changes 的 `npm audit fix --force`，留待獨立安全升級處理。

本機 Python 3.13 的原始安裝位置已不存在，現有 venv 無法啟動。依同步規範，本次 Python pytest、Mypy 與 2-opt regression 必須由推送後的 `Custom compatibility` GitHub Actions 覆蓋。

## 驗證結果

- `scripts/check-extension-boundaries.ps1`：通過
- 客製能力 backend／API／frontend／state wiring audit：39／39 通過
- 2-opt 靜態複核：保留固定起點、完整非重複 order、O(1) prefix-delta 候選評估、非對稱 matrix 與 `None` 不可達邊拒絕
- 2-opt regression 靜態複核：保留對稱／非對稱 first-improvement oracle 與不可達邊 coverage；實際 pytest 待 CI 執行
- Backend Pyright：通過，0 errors／0 warnings／0 informations
- Electron syntax：通過
- Frontend TypeScript（React 19／TypeScript 7）：通過
- Vite 8 production build：通過；僅有既存 dynamic-import 與大型 chunk 警告
- staged integration diff review：通過
- `git diff --cached --check`：通過
- conflict-marker scan：通過
- `doc/coordinates.txt`：未追蹤，且仍由 `.gitignore` 明確忽略

## 整合 commit

- sync branch：`sync/upstream-v0.2.192`
- merge commit：`6211564`
- 客製父 commit：`bef82f0`
- 官方父 commit：`a1ebe20`
- `custom/main` fast-forward 目標：`9f45017`

fast-forward 後 rehearsal 已通過：`custom/main` `7efc5af` + `upstream/main` `a1ebe20`。

## 發布與 CI

- `origin/main`：已非 force push 至官方鏡像 `a1ebe20`
- `origin/custom/main`：已非 force push 至已驗證程式碼 commit `910652a`
- `Custom compatibility`：成功
- Run：<https://github.com/Charlie-0926/locwarp/actions/runs/30529997877>
- CI head SHA：`910652a94a41323da1d045616bcf6143c920c387`

CI backend job 已通過新版 requirements 安裝、`backend/tests/extensions` pytest（包含 2-opt regression）、Mypy 與 Pyright；frontend job 已通過 `npm ci`、TypeScript 與 production build。

## 尚待實機驗證

CI 無法取代下列 Windows／iPhone 行為驗證：

- pymobiledevice3 10.1.0 的 USB 連線與 Wi-Fi tunnel（包含 Python 3.13 TLS-PSK／PyInstaller bundle）。
- 兩台 iPhone 的 multi-device auto-sync、UI mirroring、state alignment 與 leader handoff。
- Jump dwell／固定方向移動在實機 route transition、pause 與 hot-apply 下的行為。
